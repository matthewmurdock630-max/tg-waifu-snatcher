services:
  - type: web
    name: waifu-bot
    runtime: python
    rootDir: .
    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python -m bot
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
