services:
  - type: web
    name: waifu-snatcher-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false